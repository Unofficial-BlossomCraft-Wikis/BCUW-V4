---
interface Props {
  id: string;
  alt: string;
  copyurl?: string;
  copy?: boolean;
  divClassName?: string;
  imgClassName?: string;
}

const { id, alt, copyurl, copy, divClassName, imgClassName} = Astro.props;
import CDNBase from "../components/CDNBase";
---

<CDNBase url={`https://utfs.io/a/co6j4hk884/${id}`} alt={alt} copyurl={copyurl} copy={copy} divClassName={divClassName} imgClassName={imgClassName} client:only="react" />